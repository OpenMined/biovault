name: BioVault Scenario Tests

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  schedule:
    - cron: '45 2 * * *'
  workflow_dispatch:
  workflow_call:

concurrency:
  group: biovault-scenario-tests-${{ github.ref }}
  cancel-in-progress: false

jobs:
  syqure-distributed:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Checkout syqure repo
        uses: actions/checkout@v4
        with:
          repository: OpenMined/syqure
          path: ../syqure
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup workspace dependencies
        shell: bash
        run: |
          chmod +x scripts/setup-workspace.sh
          ./scripts/setup-workspace.sh

      - name: Install Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.91.0

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            cli -> target
            syftbox/rust -> target
            ../syqure -> target

      - name: Install uv
        uses: astral-sh/setup-uv@v3

      - name: Install protoc
        uses: arduino/setup-protoc@v2
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Build syqure
        run: |
          cd ../syqure
          cargo build
          ls -la target/debug/syqure

      - name: Build BioVault CLI
        run: cd cli && cargo build --release

      - name: Run syqure-distributed scenario
        run: ./test-scenario.sh --client-mode rust tests/scenarios/syqure-distributed.yaml

      - name: Show logs on failure
        if: failure()
        shell: bash
        run: |
          echo "=== Sandbox Directory Structure ==="
          find sandbox -type d -maxdepth 3 2>/dev/null || true
          echo ""
          echo "=== Client Logs ==="
          find sandbox -name "*.log" -exec echo "--- {} ---" \; -exec cat {} \; 2>/dev/null || true
          echo ""
          echo "=== MPC Channel Files ==="
          find sandbox -path "*/_mpc/*" -type f 2>/dev/null | head -50 || true

      - name: Cleanup devstack
        if: always()
        shell: bash
        run: |
          chmod +x ./tests/scripts/devstack.sh
          ./tests/scripts/devstack.sh --stop --reset || true

  # Commented out - only running syqure-distributed for now
  # run-scenarios:
  #   runs-on: ${{ matrix.os == 'windows' && 'namespace-profile-windows-medium' || 'ubuntu-latest' }}
  #   strategy:
  #     fail-fast: false
  #     matrix:
  #       include:
  #         - os: linux
  #           client_mode: go
  #           scenario: tests/scenarios/import-and-run.yaml
  #           needs_container: true
  #         - os: linux
  #           client_mode: go
  #           scenario: tests/scenarios/inbox-ping-pong.yaml
  #           needs_container: true
  #         - os: linux
  #           client_mode: go
  #           scenario: tests/scenarios/messaging-core.yaml
  #           needs_container: false
  #         - os: linux
  #           client_mode: go
  #           scenario: tests/scenarios/break-nextflow.yaml
  #           needs_container: true
  #         - os: linux
  #           client_mode: go
  #           scenario: tests/scenarios/key-management.yaml
  #           needs_container: false
  #         - os: windows
  #           client_mode: go
  #           scenario: tests/scenarios/import-and-run.yaml
  #           needs_container: true
  #         - os: windows
  #           client_mode: go
  #           scenario: tests/scenarios/inbox-ping-pong.yaml
  #           needs_container: true
  #         - os: windows
  #           client_mode: go
  #           scenario: tests/scenarios/messaging-core.yaml
  #           needs_container: false
  #         - os: windows
  #           client_mode: go
  #           scenario: tests/scenarios/break-nextflow.yaml
  #           needs_container: true
  #         - os: windows
  #           client_mode: go
  #           scenario: tests/scenarios/key-management.yaml
  #           needs_container: false
  #
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - name: Setup Podman Hyper-V (Windows)
  #       if: matrix.os == 'windows' && matrix.needs_container == true
  #       shell: pwsh
  #       run: |
  #         # ... Windows Podman setup ...
  #
  #     - name: Setup workspace dependencies
  #       shell: bash
  #       run: |
  #         chmod +x scripts/setup-workspace.sh
  #         ./scripts/setup-workspace.sh
  #
  #     - name: Install Rust
  #       uses: dtolnay/rust-toolchain@master
  #       with:
  #         toolchain: 1.91.0
  #
  #     - name: Cache Rust dependencies
  #       uses: Swatinem/rust-cache@v2
  #       with:
  #         workspaces: |
  #           cli -> target
  #           syftbox/rust -> target
  #
  #     - name: Install uv
  #       uses: astral-sh/setup-uv@v3
  #
  #     - name: Install protoc
  #       uses: arduino/setup-protoc@v2
  #       with:
  #         repo-token: ${{ secrets.GITHUB_TOKEN }}
  #
  #     - name: Setup Go
  #       uses: actions/setup-go@v5
  #       with:
  #         go-version-file: syftbox/go.mod
  #
  #     - name: Install Nextflow (Unix)
  #       if: matrix.os != 'windows'
  #       shell: bash
  #       run: |
  #         curl -fsSL https://get.nextflow.io | bash
  #         sudo mv nextflow /usr/local/bin/
  #         chmod +x /usr/local/bin/nextflow
  #         nextflow -version
  #
  #     - name: Build BioVault CLI
  #       run: cd cli && cargo build --release
  #
  #     - name: Run scenario (Unix)
  #       if: matrix.os != 'windows'
  #       shell: bash
  #       run: ./test-scenario.sh --client-mode ${{ matrix.client_mode }} ${{ matrix.scenario }}
  #
  #     - name: Run scenario (Windows Hyper-V)
  #       if: matrix.os == 'windows' && matrix.needs_container == true
  #       shell: pwsh
  #       env:
  #         CONTAINERS_MACHINE_PROVIDER: hyperv
  #         BIOVAULT_CONTAINER_RUNTIME: podman
  #         BIOVAULT_HYPERV_MOUNT: "1"
  #       run: .\win.ps1 ./test-scenario.sh --client-mode ${{ matrix.client_mode }} ${{ matrix.scenario }}
  #
  #     - name: Run scenario (Windows)
  #       if: matrix.os == 'windows' && matrix.needs_container != true
  #       shell: pwsh
  #       run: .\win.ps1 ./test-scenario.sh --client-mode ${{ matrix.client_mode }} ${{ matrix.scenario }}
  #
  #     - name: Show logs on failure
  #       if: failure()
  #       shell: bash
  #       run: |
  #         echo "=== Sandbox Directory Structure ==="
  #         find sandbox -type d -maxdepth 3 2>/dev/null || true
  #         echo ""
  #         echo "=== Client Logs ==="
  #         find sandbox -name "*.log" -exec echo "--- {} ---" \; -exec cat {} \; 2>/dev/null || true
  #
  #     - name: Cleanup devstack
  #       if: always()
  #       shell: bash
  #       run: |
  #         chmod +x ./tests/scripts/devstack.sh
  #         ./tests/scripts/devstack.sh --stop --reset || true
