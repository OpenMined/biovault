# Mapping file for custom_batch.csv
# Maps CSV column names to workflow parameters

mappings:
  sample_name: participant_id
  genome_build: ref_version
  reference_genome: ref
  reference_idx: ref_index
  alignment_file: aligned
  alignment_idx: aligned_index
  # Custom parameters that will be passed to workflow
  phenotype: phenotype
  batch: batch_id